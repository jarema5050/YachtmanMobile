import { ReactNode } from 'react';
export interface ShowModalFnProps {
    close: () => void;
}
export declare type ShowModalObject = {
    content: ReactNode;
} & Partial<Options>;
export declare type ShowModalRenderFn = (props: ShowModalFnProps) => ShowModalObject;
export declare type ShowModalFn = ShowModalRenderFn | ShowModalObject;
export interface Options {
    height: number;
    closeTreshold: number;
}
export interface ModalContextProps {
    showModal: (fn: ShowModalFn) => void;
    closeModal: () => void;
}
